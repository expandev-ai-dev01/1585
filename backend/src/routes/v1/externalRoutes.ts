/**
 * @summary
 * External API routes configuration (public endpoints)
 *
 * @module routes/v1/externalRoutes
 */

import { Router } from 'express';

const router = Router();

// Public routes will be added here
// Example: router.use('/public', publicRoutes);

export default router;
