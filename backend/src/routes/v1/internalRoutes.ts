/**
 * @summary
 * Internal API routes configuration (authenticated endpoints)
 *
 * @module routes/v1/internalRoutes
 */

import { Router } from 'express';

const router = Router();

// Internal routes will be added here
// Example: router.use('/task', taskRoutes);

export default router;
